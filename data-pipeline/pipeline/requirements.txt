# --- Critical Version Alignment ---
# Pinning these three is the ONLY way to satisfy the s3fs <-> pyiceberg conflict
fsspec==2023.12.2
s3fs==2023.12.2
pyarrow>=14.0.0

# --- Core Logic ---
# Note: s3fs is already included in the [s3fs] extra
pyiceberg[sql-postgres,s3fs,pyarrow]==0.6.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
duckdb==1.0.0
psycopg2-binary>=2.9.9
requests>=2.31.0
python-dotenv>=1.0.1
aiobotocore==2.26.0
boto3==1.41.5
botocore==1.41.5
pyyaml>=6.0.1