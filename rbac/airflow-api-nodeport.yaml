apiVersion: v1
kind: Service
metadata:
  name: airflow-api-nodeport
  namespace: airflow
spec:
  type: NodePort
  selector:
    component: api-server
    tier: airflow
    release: airflow
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080